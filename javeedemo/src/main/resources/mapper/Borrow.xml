<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.javeedemo.mapper.BorrowMapper">
    <select id="listByCondition" resultType="com.example.javeedemo.entity.Borrow">
        select * from borrow

        <where>
            (borrowstatus2='成功借阅' or borrowstatus2='已归还' or borrowstatus2='申请借阅中')
            <if test="name != null and name != ''">
                and user_name = #{name}
            </if>
        </where>
        order by id desc
    </select>
    <select id="listByCondition2" resultType="com.example.javeedemo.entity.Borrow">
        select * from borrow
        <where>
            <if test="name != null and name != ''">
                and user_name = #{name}
            </if>
        </where>
        order by id desc
    </select>
    <select id="listByCondition3" resultType="com.example.javeedemo.entity.Borrow">
        select * from borrow
        <where>
            (borrowstatus2='成功借阅' or borrowstatus2='已归还')
            <if test="name != null and name != ''">
                and user_name = #{name}
            </if>
        </where>
        order by id desc
    </select>
    <insert id="save">
        insert into borrow(book_name, user_name,user_no,book_no,borrowstatus,borrowstatus2)
        values(#{bookName},  #{userName}, #{userNo}, #{bookNo},'未归还','申请借阅中')
    </insert>

    <update id="updateById">
        update borrow set book_name = #{bookName}, book_no = #{bookNo}, user_no = #{userNo}, user_name = #{userName}, user_phone = #{userPhone}, score = #{score}, updatetime = #{updatetime} where id = #{id}
    </update>

    <update id="updateStatus">
        update borrow set status = #{status} where id = #{id}
    </update>

    <delete id="deleteById">
        delete from borrow where id = #{id}
    </delete>


    <select id="list" resultType="com.example.javeedemo.entity.Borrow">
        select * from borrow order by id desc
    </select>

    <select id="getById" resultType="com.example.javeedemo.entity.Borrow">
        select * from borrow where id = #{id}
    </select>

    <update id="update1">
        update borrow set borrowstatus = '申请归还中'
        where id = #{id}
    </update>
    <update id="update2">
        update borrow set borrowstatus = '归还成功',borrowstatus2 = '已归还',updatetime = NOW()
        where id = #{id}
    </update>

    <update id="update3">
        update borrow set statement = #{statement} where id = #{id}
    </update>
    <update id="update4">
        update borrow set borrowstatus2 = '成功借阅'
        where id = #{id}
    </update>
</mapper>