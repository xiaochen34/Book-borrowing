<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.javeedemo.mapper.UserMapper">

    <select id="list" resultType="com.example.javeedemo.entity.User">
        select *from user
    </select>
    <select id="listByCondition" resultType="com.example.javeedemo.entity.User">
        select * from user
            <where>
                <if test="name != null and name !=''">
                    name like concat('%',#{ name },'%')
                </if>
                <if test="phone != null and phone !=''">
                    and phone like concat('%',#{ phone },'%')
                </if>
            </where>
        order by id desc
    </select>
    <insert id="save">
        insert into user(name,username,age,sex,phone,address,password,state)
        value (#{name},#{username},#{age},#{sex},#{phone},#{address},#{password},'正常')
    </insert>
    <select id="getById" resultType="com.example.javeedemo.entity.User">
        select * from user where id =#{id}
    </select>
    <update id="updateById">
        update user set name=#{name},age=#{age},sex=#{sex},phone=#{phone},address=#{address},updatetime=#{updatetime}
        where id=#{id}
    </update>
    <update id="updateByName">
        update user set state='申请注销账号中'
        where name=#{name}
    </update>
    <update id="updateByName1">
        update user set state='已注销'
        where name=#{name}
    </update>
    <delete id="deleteById">
        delete from user where id=#{id}
    </delete>
</mapper>